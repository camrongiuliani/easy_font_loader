name: easy_font_loader
description: Load TTF files form network or assets.
version: 0.0.6
homepage: https://github.com/camrongiuliani/easy_font_loader

environment:
  sdk: '>=3.5.3 <4.0.0'
  flutter: ">=3.3.4"

dependencies:
  flutter:
    sdk: flutter

  flutter_cache_manager: ^3.4.1
  stash: ^4.4.1
  stash_sembast_web: ^5.1.0
  http:
  package_info_plus:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
